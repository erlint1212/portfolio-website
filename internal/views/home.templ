package views

templ Home(title string) {
    @Base("home", homeContent())
}

templ homeContent() {
    @HtmxIndicator()
    <h1>Welcome to my portfoilio page!</h1>
    <button hx-get="/projects" hx-target="#project-list" hx-swap="innerHTML" class="button">Button</button>
    <div id="project-list"></div>
}
